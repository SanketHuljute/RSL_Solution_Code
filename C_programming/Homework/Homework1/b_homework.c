#include<stdio.h>
int main()
{
    // float meant we take decimal value too
    float num;
    printf("Eneter the num : ");
    scanf("%f",&num);

    printf("The value of the cube is %f",(num*num*num));
    return 0;
}
