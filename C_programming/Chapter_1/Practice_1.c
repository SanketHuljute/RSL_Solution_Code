#include<stdio.h>
// Write a program to calculate arae of a square?

int main()
{
    float side;
    printf("Enter the value of side");
    scanf("%f",&side);

    printf("The area of the square is %f",side*side);
    return 0;
}